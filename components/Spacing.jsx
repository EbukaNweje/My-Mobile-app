import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Spacing = ({height = 40, width="100%"}) => {
  return (
    <View style={{height, width}} />

  )
}

export default Spacing

const styles = StyleSheet.create({})